// **Auth Module
// -Role Based
// -Account Authentication
// -JWT-based

export enum AuthInterface {
    RoleBased = "roleBased",
    AccountAuthentication = "accountAuthentication",
    JWT = "token"
}